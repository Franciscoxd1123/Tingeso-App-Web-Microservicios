spring:
  application:
    name: PBClient
  cloud:
    config:
      enabled: true
      url: http://localhost:8095
  datasource:
    url: jdbc:postgresql://${DB_HOST}:5432/Clients
    username: postgres
    password: MyPostgre74SQL38
  jpa:
    hibernate:
      ddl-auto: update