spring:
  application:
    name: PBClient
  cloud:
    config:
      enabled: true
      url: http://PBConfig:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/Clients
    username: postgres
    password: MyPostgre74SQL38
  jpa:
    hibernate:
      ddl-auto: update