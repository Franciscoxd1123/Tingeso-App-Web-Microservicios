spring:
    application:
        name: PBRequest
    cloud:
        config:
            enabled: true
            url: http://localhost:8081
    datasource:
        url: jdbc:postgresql://${DB_HOST}:5432/Requests
        username: postgres
        password: MyPostgre74SQL38
    jpa:
        hibernate:
            ddl-auto: update